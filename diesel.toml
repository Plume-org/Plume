# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "plume-models/src/schema.rs"
import_types = [ "diesel::sql_types::*", "crate::users::User_role" ]
